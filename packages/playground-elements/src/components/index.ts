import * as CustomElements from "./Elements";
import * as PrimitivesElements from "./Primitives";

export const Elements = {
  ...CustomElements,
  ...PrimitivesElements,
};

export { CustomElements, PrimitivesElements };
